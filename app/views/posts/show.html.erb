<div>
  <h2><%= @post.rationale %></h2>
</div>

<div>
  <h3>Overtime Amount Requested<%= @post.overtime_request %></h3>
</div>

<div>
  <h3>User: <%= @post.user.full_name %></h3>
</div>

<div>
  <h3>Date<%= @post.date %></h3>
</div>

<div>
  <h3>Current stage of Approval: <%= @post.status %></h3>
</div>


<%= link_to 'Edit', edit_post_path(@post), class: 'btn btn-primary' if policy(@post).update? %>